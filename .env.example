# Vercel sends this automatically as "Authorization: Bearer <CRON_SECRET>" to cron routes
CRON_SECRET=change-me
STAFF_EMAIL=hospitality@yourhospital.example
# Add provider keys when you replace the console stub in lib/visa.js (e.g. RESEND_API_KEY, TWILIO_*)
